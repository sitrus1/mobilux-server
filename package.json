{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "vercel-build": "echo 'Build step not required'",
    "start": "node server.js",
    "dev": "json-server-auth db.json -r routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.1",
    "json-server-auth": "^2.1.0",
    "@vercel/node": "^1.0.0"
  }
}
